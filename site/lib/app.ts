/** Where the app itself lives. Its own origin, and the only address the page ever sends anybody to. */
export const APP_URL = 'https://app.appchery.com';

/**
 * The repository. The page says the app is open source in three places; this is the one that lets a
 * reader check, and it is the link that earns the page its mentions from software directories.
 */
export const SOURCE_URL = 'https://github.com/s1lver-lining/appchery';
