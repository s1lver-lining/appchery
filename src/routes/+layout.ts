// SPA everywhere: the app must boot with no network, and Capacitor serves the
// bundle from the local filesystem where there is no server to render on.
export const ssr = false;
export const prerender = false;
