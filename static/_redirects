# The app is a client-routed SPA, so any path that is not a real file has to be answered with the
# shell rather than a 404. Cloudflare Pages matches static assets first, so this only catches routes.
/*  /index.html  200
