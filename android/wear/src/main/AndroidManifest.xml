<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature android:name="android.hardware.type.watch" />
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />

    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- The heart rate, which is the one figure of a run only the wrist can measure. -->
    <uses-permission android:name="android.permission.BODY_SENSORS" />
    <!-- Waking the screen when a run changes block, which is a cue the runner is not looking for. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="Appchery"
        android:theme="@android:style/Theme.DeviceDefault">

        <!--
            Ambient lives in the watch's own shared library rather than in the framework: without
            this the class androidx.wear talks to is simply not on the classpath, and always on does
            nothing at all while reporting nothing at all. Not required, so a device without it
            still installs the app and simply never stays on screen.
        -->
        <uses-library android:name="com.google.android.wearable" android:required="false" />
        <activity
            android:name=".KeypadActivity"
            android:exported="true"
            android:label="Appchery">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".LinkService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />
    </application>

</manifest>
